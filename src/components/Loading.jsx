// small loading message shown while the json data is being "fetched"
function Loading() {
    return (
        <div className="text-center py-20 text-gray-400">
            <p>Loading technologies...</p>
        </div>
    )
}

export default Loading
